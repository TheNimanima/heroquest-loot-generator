# Copy this file to .env.local and fill in your values

# Option A: Use your Cloudflare Worker URL (production — key is server-side)
VITE_WORKER_URL=https://heroquest-loot-generator.YOUR_SUBDOMAIN.workers.dev

# Option B: Direct Claude API key (dev only — NEVER commit .env.local)
# Get yours at: https://console.anthropic.com
VITE_API_KEY=sk-ant-...
